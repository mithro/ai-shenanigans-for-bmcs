# Downloaded firmware binaries (large files, can be re-downloaded)
downloads/
# Extracted firmware (can be regenerated from downloads)
extracted/
# Initramfs build artifacts (regenerated by build.py)
initramfs/build/
initramfs/initramfs.cpio.gz
initramfs/uInitrd-c410x
