# SPDX-License-Identifier: Apache-2.0

# Pull in the GPIO subsystem; the AST2050 G3 driver (CONFIG_GPIO_ASPEED_G3)
# then auto-enables because the devicetree has okay "aspeed,ast2050-gpio" nodes.
CONFIG_GPIO=y
