# SPDX-License-Identifier: Apache-2.0

# No subsystems needed: the SCU page is flat-mapped device memory
# (soc/aspeed_g3/ast2050/soc.c) and we read it directly with sys_read32().
# Console comes from the board defconfig (M0 polling UART backend).
