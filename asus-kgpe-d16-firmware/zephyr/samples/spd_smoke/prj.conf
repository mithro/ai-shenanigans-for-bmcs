# SPDX-License-Identifier: Apache-2.0

# Needs BOTH subsystems: GPIO to drive the QU5 mux selects (GPIOF4/F5) and I2C
# to read the DIMM SPD behind the mux. The AST2050 G3 drivers auto-enable from
# the enabled gpio1 / i2c1 devicetree nodes.
CONFIG_GPIO=y
CONFIG_I2C=y
