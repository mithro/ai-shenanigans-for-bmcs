# SPDX-License-Identifier: Apache-2.0

# Pull in the I2C subsystem; the AST2050 G3 driver (CONFIG_I2C_ASPEED_G3) then
# auto-enables because the devicetree has an okay "aspeed,ast2050-i2c" node.
# The console comes from the board defconfig (M0 polling UART backend).
CONFIG_I2C=y
