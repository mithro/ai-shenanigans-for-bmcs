# SPDX-License-Identifier: Apache-2.0

# Tickful kernel: k_msleep()/k_uptime_get() need the system clock, provided by the
# AST2050 timer driver (built with -DCONFIG_SYS_CLOCK_EXISTS=y). Console comes from
# the board defconfig (M0 polling UART backend). No extra config needed.
