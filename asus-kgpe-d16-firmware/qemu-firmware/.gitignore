# Cloned source trees and build outputs — regenerated by CI / build scripts,
# never committed. The committed delta is patches/, configs, scripts, and docs.
# qemu/qemu is a git submodule (mithro/qemu@d16-ast2050-machine); its build
# output (qemu/qemu/build/) is ignored by QEMU's own .gitignore.
qemu/build/
kernel/linux/
kernel/build/
uboot/u-boot/
uboot/build/
initramfs/build/
initramfs/out/
dropbear/
busybox/
firmware/
*.uImage
uImage*
zImage*
*.cpio
*.cpio.gz
*.dtb
u-boot.bin
u-boot
/tmp/
tmp/
